import React from 'react';

const PrimaryButton = () => {
    return (
        <div>
            <button className="  btn btn-primary bg-gradient-to-l from-primary to-secondary text-white	 uppercase font-bold	">Get Started</button>
        </div>
    );
};

export default PrimaryButton;